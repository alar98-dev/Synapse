[pytest]
DJANGO_SETTINGS_MODULE = synapse_project.test_settings
python_files = tests.py test_*.py *_tests.py
addopts = --strict-markers --no-migrations --reuse-db
markers =
    integration: mark test as a real integration test requiring external services.
    slow: mark test as slow.
