{
  "default": {
    "upload_progress_sample_rate": 0.005,
    "log_stream_progress_sample_rate": 0.005
  },
  "notes": "Rates are fractional (0.005 == 0.5%). Adjust per env; apply in telemetry SDK."
}
